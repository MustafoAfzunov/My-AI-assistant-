# Frontend - Digital Classroom

Frontend application will be added here.

The frontend will connect to the backend WebSocket endpoints:
- ws://localhost:8000/ws/{client_id} - Audio/control
- ws://localhost:8000/ws/text/{client_id} - Text data

Stay tuned!
